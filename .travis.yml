language: cpp
sudo: false
cache:
    apt: true
addons:
    apt:
        sources:
            # - deadsnakes
        - ubuntu-toolchain-r-test
        packages:
        - g++-5
        - libgmp-dev
compiler:
- g++-5
before_script:
- CXX=g++-5 make 2>&1 > make.log || { echo "!!!! ERROR !!!!"; cat make.log; false; }
script:
- CXX=g++-5 make check
# after_failure:
#     - "pwd"
#     - "cat config.log"
